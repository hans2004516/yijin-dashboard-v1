# Cloud Run Service 根網址；前端會自動加上 /v1/dashboard。
# 未設定時使用本機 /api/dashboard 測試端點。此變數不得放任何密鑰。
NEXT_PUBLIC_DASHBOARD_API_URL=https://your-dashboard-api-xxxxx-xx.a.run.app

# API Key 不在此設定。正式 API 模式會由使用者在畫面輸入，且只保留於該分頁。

# 正式網站 URL，用於社群分享預覽。
NEXT_PUBLIC_SITE_URL=https://your-private-site.example.com
